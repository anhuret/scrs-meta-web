{
  "license": "MIT",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "format": "prettier --write \"source/**/*.{js,jsx}\"",
    "lint": "prettier --check \"source/**/*.{js,jsx}\"",
    "clean": "rm -rf build dist",
    "reset": "rm -rf node_modules yarn.lock && yarn cache clean && yarn install"
  },
  "dependencies": {
    "history": "5.0.1",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-markdown": "7.0.0",
    "react-router-dom": "6.0.0-beta.1"
  },
  "devDependencies": {
    "@snowpack/plugin-dotenv": "2.1.0",
    "@snowpack/plugin-react-refresh": "2.5.0",
    "@testing-library/react": "12.0.0",
    "chai": "4.3.4",
    "prettier": "2.3.2",
    "snowpack": "3.8.6"
  }
}
